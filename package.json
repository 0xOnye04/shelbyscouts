{
  "name": "shelbyscout-workspace",
  "private": true,
  "dependencies": {
    "next": "16.2.6",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "scripts": {
    "postinstall": "npm --prefix shelbyscout install",
    "dev": "npm --prefix shelbyscout run dev",
    "build": "npm --prefix shelbyscout run build",
    "lint": "npm --prefix shelbyscout run lint",
    "start": "npm --prefix shelbyscout run start",
    "prisma:generate": "npm --prefix shelbyscout run prisma:generate",
    "db:push": "npm --prefix shelbyscout run db:push",
    "db:seed": "npm --prefix shelbyscout run db:seed",
    "db:studio": "npm --prefix shelbyscout run db:studio"
  }
}
